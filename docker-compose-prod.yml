services:
  web:
    build: .
#    command: python manage.py runserver 0.0.0.0:8000
#    command: gunicorn project.wsgi -b 0.0.0.0:8000
    command: ./start.sh
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    depends_on:
      - db
    environment:
      - 'DJANGO_SECRET_KEY=5mxi0U-PTF8etqpTqvYOO4BlHgMIOAMIIaiSBI0SzA2hDMD-UqQ'
  db:
    image: postgres:15
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    environment:
      - "POSTGRES_HOST_AUTH_METHOD=trust"

volumes:
  postgres_data:
